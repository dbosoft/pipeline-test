workflow: GitFlow/v1
assembly-versioning-scheme: MajorMinor
branches:
  release:
    increment: Patch
    prevent-increment:
      when-current-commit-tagged: true
  support:
    is-main-branch: false
